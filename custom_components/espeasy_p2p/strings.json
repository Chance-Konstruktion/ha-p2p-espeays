{
  "config": {
    "step": {
      "user": {
        "title": "ESPEasy P2P",
        "description": "Listen for ESPEasy C013 broadcasts and actively scan for peers.",
        "data": {
          "port": "UDP port",
          "unit": "Home Assistant unit number",
          "name": "Home Assistant peer name",
          "display_precision": "Decimal places shown for sensor values (0–6)"
        }
      }
    },
    "abort": {
      "single_instance_allowed": "Only one ESPEasy P2P listener can be configured."
    }
  },
  "options": {
    "step": {
      "init": {
        "title": "Switch configuration",
        "description": "Pick a task to set its GPIO pin or command template, or choose 'Save and close' when you are done. {task_count} task(s) listed.",
        "data": {
          "display_precision": "Decimal places shown for sensor values (0–6)",
          "task": "Task"
        }
      },
      "edit": {
        "title": "Edit {task} on unit {unit}",
        "description": "Set the BCM GPIO pin (the integration sends 'gpio,PIN,STATE'), or a command template — useful for stock ESPEasy with rules. Examples:\n\n• Stock ESPEasy + rule: 'event,schluessel' triggers 'On schluessel do … endon' on the node.\n• Direct helper: \"door,{state}\" (the placeholder {state} is replaced with 0 or 1).\n\nIf both fields are filled, the template wins. Tick 'Remove this entry' to delete the override entirely.",
        "data": {
          "pin": "BCM GPIO pin (optional)",
          "cmd": "Command template (optional)",
          "remove": "Remove this entry"
        }
      }
    }
  },
  "services": {
    "scan": {
      "name": "Scan for nodes",
      "description": "Broadcast a node-info packet so all reachable ESPEasy nodes immediately announce themselves."
    },
    "refetch_metadata": {
      "name": "Re-fetch task metadata",
      "description": "Force a refresh of the /json metadata for every known node. Useful after upgrading the integration so newly extracted fields (e.g. GPIO pins) are picked up without restarting Home Assistant."
    },
    "send_command": {
      "name": "Send raw command to a node",
      "description": "Send an arbitrary ESPEasy command to a specific node, both via C013 P2P (Type-0) and via HTTP /control. The HTTP response is logged at INFO so you can verify exactly what the node accepts.",
      "fields": {
        "unit": {
          "name": "Unit",
          "description": "ESPEasy unit number of the target node."
        },
        "command": {
          "name": "Command",
          "description": "Command string, exactly as you would type it after `?cmd=` in the node's web UI (e.g. `gpio,12,1`)."
        }
      }
    },
    "set_gpio_pin": {
      "name": "Set GPIO pin for a task",
      "description": "Persist a (unit, task name) → GPIO pin mapping. Needed for firmware (notably RPiEasy 'Output Helper') that does not expose the pin via /json. Once set, switch toggles use 'gpio,PIN,STATE' directly.",
      "fields": {
        "unit": {
          "name": "Unit",
          "description": "ESPEasy unit number of the target node."
        },
        "task_name": {
          "name": "Task name",
          "description": "Exact TaskName as shown in the node UI."
        },
        "pin": {
          "name": "GPIO pin",
          "description": "BCM pin number that drives the output."
        }
      }
    },
    "remove_node": {
      "name": "Remove node",
      "description": "Forget a node and remove its HA device, sensors and switches. The node will reappear if it sends a new heartbeat.",
      "fields": {
        "unit": {
          "name": "Unit",
          "description": "ESPEasy unit number to forget."
        }
      }
    }
  }
}
